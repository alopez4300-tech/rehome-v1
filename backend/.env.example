APP_NAME=Rehome
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_TIMEZONE=UTC
APP_URL=http://localhost:8000

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_LEVEL=debug

# Database (PostgreSQL recommended for production)
DB_CONNECTION=pgsql
DB_HOST=postgres
DB_PORT=5432
DB_DATABASE=rehome
DB_USERNAME=rehome
DB_PASSWORD=secret

# Redis (required for queues, cache, sessions, broadcasting)
REDIS_CLIENT=phpredis
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=null

# Cache, Sessions, Queues
CACHE_DRIVER=redis
SESSION_DRIVER=redis
QUEUE_CONNECTION=redis

# File Storage (local for dev, s3 for production)
FILESYSTEM_DISK=local
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_DEFAULT_REGION=us-east-1
# AWS_BUCKET=rehome-production-private
# AWS_URL=

# Email (Mailpit for dev, Resend for production)
MAIL_MAILER=smtp
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="no-reply@yourdomain.com"
MAIL_FROM_NAME="${APP_NAME}"

# Resend (production email)
# MAIL_MAILER=resend
# RESEND_API_KEY=

# Broadcasting (Laravel Reverb for dev, Pusher/Ably for production)
BROADCAST_CONNECTION=reverb
REVERB_APP_ID=732444
REVERB_APP_KEY=jjporfhyugshbgtmjdnw
REVERB_APP_SECRET=p7trrdu9jw8isql1yvbu
REVERB_HOST=localhost
REVERB_PORT=8080
REVERB_SCHEME=http

# Pusher (production broadcasting)
# BROADCAST_CONNECTION=pusher
# PUSHER_APP_ID=
# PUSHER_APP_KEY=
# PUSHER_APP_SECRET=
# PUSHER_HOST=api-us-east-1.pusherapp.com
# PUSHER_PORT=443
# PUSHER_SCHEME=https
# PUSHER_APP_CLUSTER=us-east-1

# Ably (alternative to Pusher)
# BROADCAST_CONNECTION=ably
# ABLY_KEY=

# Authentication & CORS
SANCTUM_STATEFUL_DOMAINS=localhost,127.0.0.1
CORS_ALLOWED_ORIGINS=http://localhost,http://localhost:3000,http://localhost:8000

# Session Security (set to true in production)
SESSION_SECURE_COOKIE=false
SESSION_SAME_SITE=lax

# AI Configuration
AI_PROVIDER=openai
AI_MODEL=gpt-4o-mini
AI_MAX_TOKENS=4096
AI_TEMPERATURE=0.7
AI_TIMEOUT=60
AI_TOKEN_SAFETY=0.10

# OpenAI
OPENAI_API_KEY=

# Anthropic (alternative to OpenAI)
# AI_PROVIDER=anthropic
# ANTHROPIC_API_KEY=

# Frontend
VITE_APP_NAME="${APP_NAME}"
VITE_REVERB_APP_KEY="${REVERB_APP_KEY}"
VITE_REVERB_HOST="${REVERB_HOST}"
VITE_REVERB_PORT="${REVERB_PORT}"
VITE_REVERB_SCHEME="${REVERB_SCHEME}"

# Optional: Monitoring & Error Tracking
# SENTRY_LARAVEL_DSN=
# TELESCOPE_ENABLED=true
